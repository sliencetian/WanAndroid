<!--articleListPage/articleListPage.wxml-->
  <block wx:for="{{listData}}" wx:key="unique">
    <view class='liststyleC'>
      <view class='listtop' id='{{item.link}}' bindtap='detail'>
        <view>
          <image class='listtopimage' src='../../images/home/head.jpg'></image>
        </view>
        <view class="listUserName">{{item.author}}</view>
        <view class='listtoptime'>{{item.niceDate}}</view>
      </view>
      <view class='title' id='{{item.link}}' bindtap='detail'>{{item.title}}</view>
      <text wx:if="{{item.desc}}" class='desc' bindtap='detail'>{{item.desc}}</text>
      <view class='listtop'>
        <view class='chapterName' id='{{item.chapterId}}' bindtap='chapter'>{{item.superChapterName}} · {{item.chapterName}}</view>
        <image wx:if="{{item.collect}}" class='collect' data-index="{{index}}" src='../../images/home/collectionSelect.png' id='{{item.id}}' bindtap='collect'></image>
        <image wx:if="{{!item.collect}}" class='collect' data-index="{{index}}" src='../../images/home/collectionNormal.png' id='{{item.id}}' bindtap='collect'></image>
      </view>
    </view>
  </block>

